
setProto = Object.setPrototypeOf or (value, proto) -> value.__proto__ = proto

module.exports = (value, type) ->
  if value?
    proto = if type is null then null else type.prototype
    setProto value, proto
  value

#
# console.log __dirname + "/index.coffee"
#
# describe "Stack()", ->
#
#   Stack = require "../.."
#
#   it "returns a stack trace for the caller's current line", ->
#     stack = Stack()
#     expect(stack[0].getLineNumber()).toBe 9
#
# describe "Stack(Object)", ->
#
#   Stack = require "../.."
#
#   it "if 'limit' is defined, the stack is limited in length", ->
#     expect(Stack(limit: 1).length).toBe 1
#
#   it "if 'skip' is defined, skip the specified number of stack frames", ->
#     _ = (i, a...) -> Stack.apply(_, a)[i].toString()
#     expect(_ 1).toBe _ 0, skip: 1
#
# describe "Stack(Error)", ->
#
#   Stack = require "../.."
#
#   it "returns a stack trace for the given error", ->
#     try
#       a = ->
#         b = ->
#           c = ->
#             throw Error "bad type"
#           c()
#         b()
#       a()
#     catch error
#       stack = Stack error
#       expect(stack[0].getLineNumber()).toBe 48

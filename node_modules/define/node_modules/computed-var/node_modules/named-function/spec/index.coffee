
describe "NamedFunction", ->

  it "creates a function with a name", ->
    f = NamedFunction "f", ->
    expect(f.name).toBe "f"

  it "prevents function hoisting", ->
    expect(f).toBe undefined
    f = NamedFunction "f", ->

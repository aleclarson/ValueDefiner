
has = (obj, key) -> Object::hasOwnProperty.call obj, key

module.exports = (obj, key, defaultValue) ->
  throw TypeError "'obj' must be an Object" unless obj instanceof Object
  throw TypeError "'key' must be a String" unless typeof key is "string"
  return defaultValue unless has obj, key
  result = obj[key]
  delete obj[key]
  result
